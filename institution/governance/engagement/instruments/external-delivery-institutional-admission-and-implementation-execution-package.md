<?xml version="1.0" encoding="UTF-8"?>
<claude_execution_package id="external-delivery-g1-institutional-admission-and-implementation">

  <metadata>
    <authority>Huerta Group LLC Founder Office</authority>
    <receiving_function>Platform &amp; Systems Development</receiving_function>
    <execution_environment>Claude Code</execution_environment>
    <initiative>External Delivery — Generation One</initiative>
    <phase>Institutional Admission and Technical Implementation</phase>
    <authorization>IMPLEMENTATION AUTHORIZED SUBJECT TO INSTITUTIONAL ADMISSION FIRST</authorization>
    <application_repository>/Users/esteban/GitHub/huerta-group/huerta-group-client-engagement</application_repository>
    <accepted_application_baseline>ab6d63ecbaedacbba31a20c5650db28862a6dfe3</accepted_application_baseline>
  </metadata>

  <mission>
    Execute External Delivery — Generation One in the authorized sequence:

    1. verify current repository state;
    2. admit the already-accepted External Delivery model into the institutional repository using existing institutional-governance conventions;
    3. validate that institutional admission;
    4. only after successful institutional admission, implement External Delivery Generation One in the Headquarters Client Engagement application;
    5. validate the complete implementation without contacting Docker;
    6. commit and push only authorized work;
    7. return a complete technical completion report to Founder Office.

    Founder Office has already made the institutional and product decisions.

    Claude is executing those decisions.

    Claude must NOT:
    - invent institutional doctrine;
    - make Founder-reserved decisions;
    - reopen the accepted architecture;
    - expand Generation One;
    - substitute implementation convenience for accepted semantics.
  </mission>

  <accepted_architecture_status>
    External Delivery Generation One Technical Architecture is ACCEPTED.

    The following six corrections are authoritative and supersede conflicting details from the earlier candidate architecture:

    A. Same-party references must preserve record_class consistency.

    B. Current provider-capability maturity, sourcing posture, and current scoping disposition are DERIVED from authoritative history rather than stored as duplicate projections.

    C. paid_scoping_indicated does NOT close the scoping. Only an appropriate terminal disposition such as do_not_pursue closes it.

    D. Persist only the six readiness concerns requiring independent state. Derive:
       - underlying client authorization;
       - assignment-specific scope;
       - acceptance basis;
       from existing authoritative structures.

    E. Separate provider identity from provider detail so engagement-granted reads expose only the minimum provider identity required for assignment context.

    F. The Headquarters destination is:
       Providers
       routed at:
       /providers

    No further architecture cycle is authorized or required for A–F.
  </accepted_architecture_status>

  <controlling_semantics>
    Preserve:

    Capability Network
    →
    Implementation Scoping &amp; Sourcing
    →
    External Delivery Assignment
    →
    Provider Performance Learning

    And preserve all distinctions:

    Discovery ≠ qualification

    Evidence ≠ evaluation

    Candidate consideration ≠ assignment

    Commercial readiness ≠ Commercial Proposal

    External Assignment ≠ Authorized Work

    Provider completion ≠ Huerta Group acceptance

    Huerta Group acceptance ≠ Work Product

    Work Product ≠ Deliverable

    Deliverable ≠ client acknowledgement

    Provider performance evidence ≠ automatic maturity advancement

    External execution = ordinary delivery option, not mandate

    Founder possession of capability ≠ Founder execution requirement
  </controlling_semantics>

  <provider_maturity_doctrine>
    Capability-specific maturity remains:

    Discovered
    →
    Evaluated
    →
    Tested
    →
    Proven

    Maturity is:
    - capability-specific;
    - evidence-based;
    - contextual;
    - reversible;
    - derived from authoritative evaluation history.

    Proven requires:
    1. relevant first-party evidence from accepted actual external client delivery; AND
    2. a subsequent explicit Huerta Group Provider Evaluation citing the relevant evidence.

    Performance evidence alone must NEVER automatically advance maturity.

    Do not create:
    - universal provider grades;
    - scores;
    - star ratings;
    - rankings;
    - approved-vendor badges;
    - automatic maturity promotion.
  </provider_maturity_doctrine>

  <phase_0_application_preflight>
    Before changing ANY repository:

    Inspect the application repository:

    /Users/esteban/GitHub/huerta-group/huerta-group-client-engagement

    Establish:
    - current branch;
    - current full HEAD SHA;
    - origin/main full SHA;
    - ahead/behind relationship;
    - relationship to accepted baseline
      ab6d63ecbaedacbba31a20c5650db28862a6dfe3;
    - staged files;
    - unstaged files;
    - untracked files.

    At validation, accepted state was:
    - branch main;
    - HEAD = origin/main;
    - no staged changes;
    - no unstaged changes;
    - four unrelated pre-existing untracked files.

    Those known untracked files are:
    - ADMIN01 counsel package;
    - two OSA .docx files under docs/;
    - iDoRecruit CSV.

    Do NOT:
    - modify;
    - delete;
    - rename;
    - move;
    - stage;
    - commit;
    - inspect their substantive contents unnecessarily.

    Preserve them exactly.

    If current main has advanced from the accepted baseline:
    - inspect the intervening commits;
    - determine whether they materially conflict with the accepted architecture;
    - if compatible, use current main as the implementation baseline and report the delta;
    - if materially incompatible, STOP before institutional/application changes.

    Blocker wording:

    EXTERNAL DELIVERY GENERATION ONE IMPLEMENTATION BLOCKED — APPLICATION BASELINE RECONCILIATION REQUIRED: [precise conflict]

    Do NOT reset the repository merely to restore the old SHA.
  </phase_0_application_preflight>

  <phase_1_institutional_repository_inspection>
    Before application implementation, inspect the actual Huerta Group institutional repository.

    Locate it through the authorized Huerta Group repository structure.

    Inspect only what is necessary to establish:
    - current branch/HEAD/origin relationship;
    - worktree state;
    - institutional documentation organization;
    - existing standards convention;
    - Founder determination convention;
    - institutional documentation index;
    - metadata manifest;
    - capability-registry.json if present;
    - client-delivery-systems architecture;
    - operations-systems architecture;
    - applicable validation commands;
    - naming/identifier conventions.

    Do NOT assume:
    - filename;
    - standard identifier;
    - Founder determination identifier;
    - directory;
    - classification;
    - manifest format;
    - registry update requirement.

    Derive those from repository precedent.
  </phase_1_institutional_repository_inspection>

  <institutional_classification_rule>
    Founder Office's present determination is:

    External Delivery belongs within existing client-delivery-systems and operations-systems architecture unless actual repository conventions demonstrate otherwise.

    Do NOT create a new institutional domain merely because External Delivery is new.

    Do NOT create a new organizational function.

    If repository inspection reveals that choosing the correct institutional classification would require a NEW Founder-reserved decision not already resolved by this authorization, STOP before admission and return:

    EXTERNAL DELIVERY GENERATION ONE IMPLEMENTATION BLOCKED — FOUNDER INSTITUTIONAL DETERMINATION REQUIRED: [exact decision]

    Do not use this blocker merely because repository terminology differs from the directive. Apply existing conventions where the accepted meaning is clear.
  </institutional_classification_rule>

  <phase_2_institutional_admission>
    Using existing institutional-governance conventions, record the already-made Founder Office determination.

    Required institutional work:

    1. create the bounded institutional standard representing External Delivery Generation One;
    2. create the corresponding Founder determination record;
    3. update the institutional documentation index;
    4. update the institutional metadata manifest as required;
    5. determine from repository truth whether capability-registry.json requires amendment;
    6. if required by existing convention, amend it minimally;
    7. run institutional repository validation;
    8. inspect the resulting diff;
    9. commit and push the institutional admission before application implementation.

    This is recording Founder authority already exercised.

    Do not phrase the record as a proposal or Claude/Platform &amp; Systems Development decision.
  </phase_2_institutional_admission>

  <institutional_standard_content>
    The institutional standard must preserve the governing model without importing unnecessary database/UI implementation detail.

    It should establish, using existing institutional-document conventions:

    - purpose and scope;
    - Capability Network;
    - Implementation Scoping &amp; Sourcing;
    - External Delivery Assignment;
    - Provider Performance Learning;
    - provider types without preference hierarchy;
    - incomplete records and Unknown;
    - discovery provenance distinct from qualification;
    - capability-specific evidence/evaluation/maturity;
    - Discovered → Evaluated → Tested → Proven;
    - capability-specific/reversible maturity;
    - first-party accepted-delivery evidence + explicit later evaluation requirement for Proven;
    - external execution as ordinary option, not mandate;
    - implementation sourcing/scoping model;
    - delivery-method discretion;
    - ordinary proposal work versus paid-scoping substance test;
    - no fixed hour/fee threshold;
    - planning economics versus QuickBooks accounting truth;
    - External Assignment connection to existing Authorized Work;
    - readiness categories;
    - provider completion versus Huerta Group acceptance;
    - Work Product/Deliverable separation;
    - performance-learning feedback;
    - existing authority boundaries;
    - Generation One exclusions.

    Do NOT copy:
    - SQL design;
    - exact table names unless repository institutional convention requires technical references;
    - route implementation;
    - component details;
    - pgTAP mechanics;
    - trigger implementation;
    - migration implementation.

    Institutional doctrine should remain durable if implementation details later evolve.
  </institutional_standard_content>

  <institutional_founder_determination>
    The Founder determination record must truthfully establish that Founder Office:

    - accepted the External Delivery Generation One operating model;
    - accepted the Generation One Product Definition;
    - accepted the validated Technical Architecture with corrections A–F;
    - admitted External Delivery into the institutional model;
    - authorized bounded technical implementation.

    Preserve provenance according to existing repository convention.

    Do not manufacture historical dates beyond what current execution/repository evidence supports.
  </institutional_founder_determination>

  <institutional_registry_rule>
    Inspect capability-registry.json if it exists.

    Amend it ONLY if existing registry semantics require External Delivery to be represented there.

    Do not add it merely because the file exists.

    Report:
    - whether amendment was required;
    - repository basis for that determination;
    - exact registry change if made.
  </institutional_registry_rule>

  <institutional_admission_gate>
    Application implementation MUST NOT begin until:

    - institutional standard exists;
    - Founder determination exists;
    - documentation index is coherent;
    - metadata manifest is coherent;
    - capability registry is correctly handled;
    - institutional validation passes to the repository's prior extent;
    - admission diff has been inspected;
    - admission has been committed;
    - admission has been pushed if normal upstream is available.

    Record:
    - institutional repository starting SHA;
    - institutional admission commit SHA;
    - origin/main relationship after push;
    - changed files;
    - identifiers assigned to the standard and determination.

    If institutional admission fails, STOP.

    Do not implement the application first and promise to reconcile institutional state later.
  </institutional_admission_gate>

  <phase_3_application_architecture_record>
    After institutional admission succeeds, return to:

    /Users/esteban/GitHub/huerta-group/huerta-group-client-engagement

    Create ADR-0024 using the repository's existing ADR convention.

    ADR-0024 must record the already-accepted technical decisions concerning:

    - External Delivery placement within existing client_engagement schema as a logically distinct bounded domain;
    - External Provider as a second record_class root;
    - separate provider identity and provider detail;
    - optional same-party references with record_class consistency;
    - execution-method representation distinct from client Deliverable delivery method;
    - capability-specific evidence/evaluation/maturity;
    - authoritative history as source of current maturity/posture/scoping disposition;
    - extraction/separation implications.

    ADR-0024 must NOT:
    - reopen the product definition;
    - invent institutional doctrine;
    - expand Generation One;
    - create a new schema;
    - create a new Supabase project.
  </phase_3_application_architecture_record>

  <phase_4_forward_migrations>
    Implement the accepted bounded forward-only migration approach.

    Inspect current migration sequence first and choose the next valid migration identifiers.

    Do NOT edit applied migrations.

    Preserve:
    - client_engagement schema;
    - existing schema_migrations doctrine;
    - hosted shared prelaunch architecture;
    - future extraction compatibility.

    The validated architecture anticipated a bounded migration split between vocabulary and records/invariants.

    Use that split if current repository conventions still support it.

    If implementation mechanics require a different small forward-only split while preserving identical architecture, document the reason.

    Do not create migration fragmentation merely for convenience.
  </phase_4_forward_migrations>

  <capability_network_schema>
    Implement the minimum persisted state necessary for:

    1. provider identity;
    2. provider detail;
    3. capability;
    4. provider-capability relationship;
    5. provider evidence;
    6. provider evaluation;
    7. evaluation/evidence citation relationship.

    Use exact repository-consistent naming determined during implementation.

    Preserve logical concepts even if final technical names differ from the earlier candidate architecture.
  </capability_network_schema>

  <provider_identity_detail_split>
    Correction E is mandatory.

    Provider identity must contain only the minimum stable identity/classification state necessary for:
    - provider reference;
    - assignment context;
    - authentic/synthetic separation;
    - minimum engagement-granted identification.

    Provider detail must contain broader Headquarters-only provider information such as, where accepted:
    - discovery provenance;
    - contact information;
    - location;
    - descriptive detail;
    - broader provider metadata.

    Do not expose provider detail merely because an engagement-granted operator can read an assignment.

    RLS must preserve least privilege.
  </provider_identity_detail_split>

  <same_party_consistency>
    Correction A is mandatory.

    Optional same-party references may associate provider identity with an existing client-world:
    - Organization; OR
    - Person;

    where the same real-world party legitimately exists in both institutional roles.

    Enforce:
    - no more than the permitted same-party relationship cardinality;
    - provider authentic/synthetic record_class consistency with referenced Organization/Person;
    - no automatic authority inheritance;
    - no automatic engagement access;
    - no automatic provider qualification;
    - no client-domain mutation.

    Same-party reference = identity correspondence only.
  </same_party_consistency>

  <capability_vocabulary>
    Implement a flat operator-curated capability vocabulary sufficient for Generation One.

    Do not build:
    - hierarchical skills ontology;
    - generalized taxonomy engine;
    - tags marketplace;
    - automated matching vocabulary.

    Capability identity must be stable enough for provider-capability evidence and sourcing requirements.
  </capability_vocabulary>

  <provider_evidence>
    Implement durable Provider Evidence supporting the accepted bounded evidence categories, including:
    - discovery/supporting evidence where appropriate;
    - work sample/reference evidence;
    - bounded Huerta Group test;
    - assignment-performance evidence;
    - other capability-relevant evidence allowed by the accepted model.

    Evidence must preserve:
    - provider;
    - applicable capability where relevant;
    - evidence kind;
    - context;
    - source/provenance;
    - observation/reference;
    - relevant date where applicable;
    - recorder/time provenance;
    - correction relationship where needed.

    Evidence history must be append-only.

    No DELETE.

    Corrections must preserve the prior record.
  </provider_evidence>

  <bounded_non_client_test>
    Implement bounded Huerta Group test evidence sufficient to record:
    - test purpose;
    - test requirement;
    - test result;
    - capability tested;
    - provenance.

    Test evidence:
    - is not client work;
    - is not an External Assignment;
    - does not automatically make a capability Proven.
  </bounded_non_client_test>

  <provider_evaluation>
    Provider Evaluation must be append-only and capability-specific.

    It should preserve the accepted bounded evaluation context:
    - maturity;
    - sourcing posture;
    - strengths;
    - limitations;
    - material unknowns;
    - risk considerations;
    - rationale;
    - evaluator/time provenance;
    - cited evidence.

    Current maturity and sourcing posture MUST be DERIVED from authoritative evaluation history.

    Correction B prohibits duplicate mutable/current projection columns serving as second truth.

    Do not use a transaction-local projection mechanism to store current maturity/posture.

    Provide repository-consistent query/view/domain-read logic to obtain the current evaluation deterministically.
  </provider_evaluation>

  <maturity_invariants>
    Enforce at database level where accepted architecture requires:

    Tested:
    - requires relevant cited Huerta Group test evidence for the same provider capability.

    Proven:
    - requires relevant cited assignment_performance evidence;
    - underlying External Assignment must have reached Huerta Group accepted disposition;
    - evidence must correspond to the same provider capability;
    - explicit Provider Evaluation must establish Proven after that evidence exists.

    Later explicit evaluation may lower maturity.

    Historical evidence/evaluations remain intact.
  </maturity_invariants>

  <phase_5_scoping_sourcing>
    Implement Implementation Scoping &amp; Sourcing attached to existing Possible Work / opportunity.

    Support optional legitimate Engagement context only when that Engagement originates from the same Opportunity under existing semantics.

    Implement minimum structures for:
    - implementation scoping;
    - scoping requirements;
    - required capabilities;
    - material unknowns;
    - candidate resource consideration;
    - append-only scoping disposition history.

    Do not require OSA.

    Do not create a generalized sales pipeline.
  </phase_5_scoping_sourcing>

  <scoping_requirements>
    Requirements must support the accepted bounded planning information:
    - outcome/component;
    - constraints;
    - dependencies;
    - required capability/capabilities;
    - candidate execution methods;
    - selected method where determined;
    - external-resource need/status;
    - expected external cost where known;
    - expected direct cost where known;
    - economics basis;
    - risk/control considerations.

    Unknown must remain representable.

    Missing information must not be coerced into false certainty.
  </scoping_requirements>

  <candidate_consideration>
    Implement requirement-specific provider-capability consideration supporting:
    - reason considered;
    - suitability;
    - material unknowns;
    - quotation where known;
    - quote date/validity/source where applicable;
    - risks/dependencies;
    - selection posture;
    - selection rationale where applicable.

    Candidate consideration ≠ qualification.

    Candidate consideration ≠ assignment.

    Candidate consideration ≠ automatic provider selection.

    Enforce authentic/synthetic non-mixing.
  </candidate_consideration>

  <execution_method>
    Implement the accepted execution architecture vocabulary distinct from existing Deliverable delivery_method.

    Support at minimum:

    - founder_internal;
    - ai_software_assisted_internal;
    - external_subcontractor;
    - hybrid;
    - client_retained_provider_coordination;
    - independently_retained_professional;
    - unresolved.

    Preserve the semantic distinction:

    execution method = how Huerta Group contemplates/performs the work

    delivery method = how an existing Deliverable reaches the client
  </execution_method>

  <planning_economics>
    Provider quotations, expected external cost, expected direct cost and related sourcing economics are planning facts.

    They are NOT accounting truth.

    Preserve repository accounting guards while allowing the explicitly authorized planning fields.

    Clearly label/comment/display them as planning figures.

    Do NOT create:
    - bills;
    - expenses;
    - liabilities;
    - payments;
    - accounts payable;
    - provider invoices;
    - client invoices;
    - accounting actuals.

    QuickBooks remains authoritative.
  </planning_economics>

  <scoping_disposition>
    Implement append-only scoping disposition history.

    Current disposition MUST be DERIVED from authoritative history.

    Do not store duplicate mutable current_disposition state.

    Support accepted states:

    - being_scoped;
    - unresolved;
    - ready_for_commercial_decision;
    - paid_scoping_indicated;
    - do_not_pursue.

    Correction C is mandatory:

    paid_scoping_indicated DOES NOT close the scoping.

    It records that reliable pricing requires separately authorized professional scoping.

    It does NOT:
    - authorize paid scoping;
    - create an Engagement;
    - create Authorized Work;
    - create a Commercial Proposal;
    - terminate the underlying contemplated implementation.

    Only an appropriate terminal disposition such as do_not_pursue may close the scoping under this Generation One model.

    No sourcing state automatically creates or pre-populates a Commercial Proposal.
  </scoping_disposition>

  <commercial_proposal_boundary>
    Existing Commercial Proposal remains authoritative.

    When scoping is ready_for_commercial_decision, UI may provide contextual navigation to the existing Commercial Proposal workflow.

    Do not:
    - auto-create proposal;
    - auto-populate proposal;
    - mutate proposal state;
    - infer Founder commercial approval.
  </commercial_proposal_boundary>

  <phase_6_external_assignment>
    Implement External Delivery Assignment against:
    - existing Engagement;
    - existing Authorized Work.

    Assignment creation requires:
    - referenced Authorized Work belongs to referenced Engagement;
    - Engagement is presently eligible for execution under existing authoritative semantics;
    - authentic/synthetic class compatibility;
    - provider capability reference;
    - bounded scope;
    - acceptance basis;
    - execution method.

    Do not modify client authority merely to make an assignment possible.
  </phase_6_external_assignment>

  <assignment_authority_boundary>
    External Assignment does NOT create:
    - Engagement;
    - client authority;
    - Client Decision;
    - Authorized Work;
    - Work Product;
    - Deliverable;
    - custody;
    - acknowledgement;
    - closure.

    It executes a bounded component of already-authorized Huerta Group work.
  </assignment_authority_boundary>

  <readiness_model>
    Correction D is mandatory.

    Persist exactly the six independent readiness concerns:

    1. client-side authority for the external delivery arrangement;
    2. provider-side contractual basis;
    3. confidentiality / data handling;
    4. IP / work-product chain;
    5. security / system / physical-access controls;
    6. licensing / professional restrictions.

    Derive the other three readiness concerns:

    7. underlying client authorization
       → derive from existing authoritative Client Engagement structures.

    8. assignment-specific scope
       → derive from the External Assignment's bounded scope / authoritative assignment structure.

    9. acceptance basis
       → derive from the External Assignment's explicit acceptance basis.

    Do NOT persist duplicate gate rows for 7–9.
  </readiness_model>

  <readiness_semantics>
    For the six persisted gates support repository-consistent bounded state sufficient for:
    - unresolved;
    - established;
    - not applicable;
    - blocked.

    Unresolved MUST NEVER mean satisfied.

    Seed the six persisted concerns at assignment creation.

    They must:
    - remain attached to the assignment;
    - be undeletable through ordinary operator paths;
    - not move between assignments;
    - preserve provenance.

    Where established basis exists in an authoritative Headquarters record, reference it using the most structured repository-supported reference possible.

    Where authoritative custody is external to the application, preserve an appropriate bounded reference/note.

    Do not duplicate legal records.

    Software records asserted basis.

    Software does NOT establish legal sufficiency.
  </readiness_semantics>

  <assignment_ready_derivation>
    Assignment readiness must be DERIVED.

    Ready requires:
    - six persisted independent readiness concerns satisfied or legitimately not applicable;
    - underlying client authorization currently valid under existing authoritative structures;
    - bounded assignment scope present/valid;
    - acceptance basis present/valid;
    - Engagement/Authorized Work still eligible for execution.

    Do not store a separate mutable "ready" truth.
  </assignment_ready_derivation>

  <assignment_state_model>
    Implement the smallest accepted execution state necessary to support:

    preparation
    →
    readiness
    →
    execution
    →
    provider completion
    →
    Huerta Group review
    →
    Accepted
    OR
    Rework Required
    OR
    Rejected / Escalated

    Include cancellation only to the bounded extent validated by existing Engagement closure semantics.

    Assignment state transitions must be explicit and database-enforced.

    No automatic transitions from unrelated records.
  </assignment_state_model>

  <assignment_events>
    Preserve append-only execution/review history.

    At minimum support events necessary for:
    - beginning execution;
    - provider completion;
    - Huerta Group review;
    - rework disposition;
    - subsequent provider completion;
    - subsequent review;
    - rejection/escalation;
    - cancellation where permitted.

    Rework must preserve:
    - prior provider completion;
    - prior review;
    - deficiency/rework reason;
    - later resubmission;
    - final review outcome.

    No destructive rewrite.
  </assignment_events>

  <completion_acceptance_boundary>
    Preserve:

    PROVIDER COMPLETION
    ≠
    HUERTA GROUP ACCEPTANCE

    Provider completion means only that the provider has represented/submitted the assigned output as complete for Huerta Group review.

    Huerta Group acceptance requires an explicit review disposition.

    Acceptance must NOT automatically create:
    - Work Product;
    - Deliverable;
    - custody;
    - client acknowledgement;
    - Client Decision;
    - Authorized Work;
    - closure.
  </completion_acceptance_boundary>

  <accepted_output_link>
    Accepted external output may subsequently be associated with a Work Product created through the EXISTING authoritative Work Product act.

    Enforce:
    - assignment accepted first;
    - Work Product belongs to the appropriate existing Authorized Work/Engagement context;
    - External Delivery does not create a parallel Work Product primitive.

    Linking accepted output ≠ creating the Work Product.
  </accepted_output_link>

  <phase_7_provider_performance>
    After appropriate assignment disposition, permit creation of assignment_performance Provider Evidence for the relevant provider capability.

    Preserve observations sufficient for:
    - requirement satisfaction;
    - quality/rework;
    - timeliness;
    - communication;
    - security/access compliance;
    - integration burden;
    - problems;
    - Huerta Group acceptance outcome;
    - would-consider-again posture.

    Performance evidence must be historical evidence.

    It must NOT automatically:
    - change maturity;
    - change sourcing posture;
    - create a provider grade;
    - create automatic provider preference.

    A later explicit Provider Evaluation may cite the evidence and establish current maturity/posture.
  </phase_7_provider_performance>

  <phase_8_rls>
    Enable and FORCE RLS for every new External Delivery table as required by repository doctrine.

    Preserve deny-by-default.

    Capability Network writes remain appropriately restricted to Founder Office/internal authority under existing operator doctrine.

    Correction E requires strict read separation:

    Engagement-granted access to an External Assignment may expose only the minimum Provider Identity necessary to operate that assignment.

    It must NOT automatically expose:
    - provider detail;
    - discovery provenance;
    - contact detail beyond specifically justified assignment need;
    - unrelated capabilities;
    - Provider Evidence;
    - Provider Evaluation;
    - other assignments;
    - network-wide sourcing posture.

    Provider Evidence/Evaluation remain restricted according to accepted Headquarters authority.

    Scoping access must follow the accepted Opportunity/Engagement authority pattern without broadening client access.

    External Assignment access must remain engagement-scoped for authorized internal operators.

    No Client Platform access.

    No client-user access.

    No public access.

    No Storage change.
  </phase_8_rls>

  <phase_9_provenance_history>
    Apply repository-standard trigger-set provenance.

    Caller-supplied provenance must not override authoritative operator/time provenance.

    Preserve:
    - authored/recorded by;
    - authored/recorded at;
    - revised by/at where mutable current records legitimately permit revision;
    - pinned parent relationships;
    - append-only evidence;
    - append-only evaluations;
    - append-only scoping dispositions;
    - append-only assignment events.

    No ordinary DELETE.

    Use SECURITY INVOKER functions and repository-standard search_path protections where transactional acts require functions.
  </phase_9_provenance_history>

  <phase_10_build01>
    Revise BUILD01 only to the bounded extent necessary to recognize Provider as a second legitimate record_class root.

    Preserve the doctrine:

    record_class belongs to legitimate root identity and downstream records inherit/derive classification rather than proliferating independent flags.

    Add assertions proving:
    - provider is a legitimate root;
    - provider record_class is write-once;
    - same-party references require record_class consistency;
    - downstream External Delivery records do not introduce independent record_class flags unnecessarily;
    - authentic/synthetic mixing is refused.

    Do not weaken BUILD01 generally.
  </phase_10_build01>

  <phase_11_domain_reads>
    Implement repository-consistent domain reads for:
    - provider list;
    - provider identity/detail;
    - provider capabilities;
    - current capability maturity/posture derived from latest evaluation;
    - evidence history;
    - evaluation history;
    - scoping;
    - requirements;
    - unknowns;
    - candidates;
    - current scoping disposition derived from history;
    - assignments;
    - derived readiness;
    - assignment event/review history;
    - performance evidence.

    Preserve read-fidelity invariant:

    successful zero rows = empty

    failed/refused/indeterminate read ≠ empty

    Never coerce authoritative read failure into false certainty.

    Add read-contract coverage for all new authoritative reads.
  </phase_11_domain_reads>

  <phase_12_actions_labels>
    Implement actions using existing Headquarters doctrine:
    - deliberate recording acts;
    - refusal surfaced to operator;
    - redirect/back behavior;
    - no silent mutation;
    - no inferred consequential acts.

    Add bounded labels/state presentation consistent with current labels.ts conventions.

    Do not introduce a generalized workflow framework.
  </phase_12_actions_labels>

  <phase_13_providers_surface>
    Implement the accepted top-level Headquarters destination:

    Providers

    Route:

    /providers

    Required Generation One surface:
    - provider list;
    - provider creation;
    - provider detail;
    - capabilities;
    - capability-specific current maturity/posture;
    - evidence;
    - evaluations;
    - bounded recording actions.

    Use existing Headquarters:
    - component system;
    - visual hierarchy;
    - state-first doctrine;
    - Recording patterns;
    - refusal patterns;
    - read-fidelity behavior.

    Do not create:
    - dashboard;
    - marketplace;
    - procurement center;
    - ranking interface.

    Provider list may support only bounded practical filtering necessary for authentic use.

    Do not turn filtering into provider matching.
  </phase_13_providers_surface>

  <provider_surface_semantics>
    Discovery source must remain visually/semantically separate from qualification/maturity.

    Unknown values should normally be omitted or truthfully represented rather than replaced with invented certainty.

    A provider may exist with incomplete information.

    Do not require full profile completion before use.
  </provider_surface_semantics>

  <phase_14_scoping_surface>
    Add contextual Scoping &amp; Sourcing to the existing Possible Work experience.

    Preserve existing Possible Work architecture.

    Surface should make understandable:
    - contemplated implementation/outcome;
    - requirements;
    - required capabilities;
    - candidate execution methods;
    - material unknowns;
    - candidate resources;
    - quotations/planning economics;
    - risks/dependencies;
    - delivery architecture;
    - current disposition derived from history;
    - commercial readiness;
    - paid-scoping indication.

    When legitimate Engagement context exists, provide bounded contextual reference.

    Do not duplicate Engagement.

    Do not create a generalized implementation-planning application.
  </phase_14_scoping_surface>

  <phase_15_assignment_surface>
    Add External Assignments contextually beneath the relevant Engagement / Authorized Work work area according to actual current Headquarters IA.

    Required operator comprehension:
    - Authorized Work being supported;
    - external provider;
    - provider capability;
    - execution method;
    - bounded scope;
    - acceptance basis;
    - six persisted readiness concerns;
    - three derived readiness concerns;
    - overall derived readiness;
    - execution state;
    - provider completion;
    - review/rework history;
    - Huerta Group acceptance disposition;
    - performance-learning action when eligible.

    Provide a bounded assignment detail surface if current application patterns require it.

    Preserve navigation back to:
    - Engagement;
    - Authorized Work;
    - Provider;
    - originating scoping/requirement where applicable.

    No generalized project-management surface.
  </phase_15_assignment_surface>

  <phase_16_guides_navigation>
    Add only the minimum Operating Guides required for safe authentic use, following current repository guide conventions.

    Expected subjects may include:
    - recording a provider;
    - scoping an implementation;
    - assigning work externally.

    Use actual guide taxonomy and naming conventions.

    Update Headquarters navigation to add:

    Providers

    at the accepted appropriate top-level position determined by current shell structure.

    Do not otherwise reorganize navigation.

    Update return routing only where required for new bounded flows.
  </phase_16_guides_navigation>

  <phase_17_documentation>
    Add/update bounded implementation documentation according to repository convention.

    Include:
    - External Delivery technical/domain documentation;
    - separation/extraction implications;
    - authoritative institutional-standard reference;
    - ADR-0024 reference.

    Preserve future separation readiness.

    Do not create a second evolving product definition inside the application repository.
  </phase_17_documentation>

  <separation_readiness>
    External Delivery remains in client_engagement schema for Generation One.

    Preserve the ability to extract/migrate the bounded capability later with the application/domain if architecture changes.

    Document:
    - owned tables;
    - functions;
    - enums;
    - policies;
    - dependencies on Organization/Person same-party references;
    - Opportunity;
    - Engagement;
    - Authorized Work;
    - Work Product;
    - Auth/operator assumptions;
    - migration order;
    - validation dependencies.

    No second Supabase project is authorized.

    No production infrastructure is authorized.
  </separation_readiness>

  <phase_18_pgtap>
    Add validated External Delivery pgTAP coverage.

    At minimum prove:

    - FORCE RLS on every new table;
    - no ordinary DELETE;
    - least-privilege policies;
    - provider identity/detail read separation;
    - provider detail/evidence/evaluation not leaked through engagement grant;
    - provenance overwrite;
    - append-only evidence;
    - append-only evaluation;
    - append-only scoping disposition;
    - append-only assignment events;
    - provider record_class write-once;
    - same-party record_class consistency;
    - authentic/synthetic non-mixing;
    - Tested requires qualifying test evidence;
    - Proven requires accepted assignment-performance evidence plus explicit evaluation;
    - performance evidence alone does not advance maturity;
    - maturity/posture derived from evaluation history;
    - scoping current disposition derived from history;
    - paid_scoping_indicated leaves scoping open;
    - do_not_pursue can terminally close scoping;
    - assignment Authorized Work belongs to Engagement;
    - assignment creation refused where Engagement is not eligible for execution;
    - six persisted readiness concerns seeded;
    - six readiness rows cannot be deleted/moved;
    - unresolved readiness is not satisfied;
    - three additional readiness concerns are derived rather than duplicated;
    - execution before readiness refused;
    - provider completion does not equal acceptance;
    - rework preserves history;
    - repeated completion/review cycles remain reconstructable;
    - accepted-output link refused before Huerta Group acceptance;
    - accepted-output link remains within correct Authorized Work/Engagement;
    - planning economics remain operational facts without accounting-state creation;
    - no External Delivery act automatically creates Commercial Proposal, Authorized Work, Work Product, Deliverable, acknowledgement or closure.
  </phase_18_pgtap>

  <phase_19_synthetic_probes>
    Implement/run the three bounded synthetic probe groups using repository-standard disposable synthetic/PLACEHOLDER fixtures.

    Probe 1 — Capability Network:
    validate authentic-use scenarios including:
    - newly discovered freelancer;
    - Tucson business independently represented from any TDD relationship;
    - bounded non-client test;
    - capability-specific evidence/evaluation.

    Probe 2 — Scoping &amp; Sourcing:
    validate:
    - straightforward implementation sourcing;
    - quotations/planning economics;
    - commercial readiness;
    - paid-scoping indication;
    - hybrid delivery;
    - no automatic Commercial Proposal.

    Probe 3 — External Assignment:
    validate:
    - Authorized Work attachment;
    - readiness;
    - execution;
    - provider completion;
    - rework;
    - resubmission;
    - Huerta Group acceptance;
    - accepted-output linkage;
    - provider-performance evidence;
    - subsequent explicit evaluation.

    Use synthetic-exercise records only.

    Do not fabricate authentic client/provider state.

    Probes must verify that consequential acts which are NOT authorized remain absent.
  </phase_19_synthetic_probes>

  <phase_20_manifest>
    Regenerate the ownership manifest using the repository's existing supported command if required by implementation.

    Do not hand-edit generated ownership state where repository tooling owns it.

    Confirm External Delivery objects are represented within the accepted existing capability/schema boundary.

    Do not modify unrelated Client Platform ownership merely to make verify:full green.
  </phase_20_manifest>

  <known_unrelated_verification_defect>
    A previously identified Client Platform ownership-manifest verification defect involving:

    cp_material_retrieval_*

    was reported as pre-existing and outside External Delivery.

    During final validation:

    - independently determine whether it still exists;
    - if it exists, report it as pre-existing/out-of-scope;
    - do NOT repair it;
    - do NOT rename it;
    - do NOT suppress it;
    - do NOT modify unrelated ownership state to hide it.

    Separately establish whether External Delivery introduced any new verification failure.
  </known_unrelated_verification_defect>

  <docker_prohibition>
    Docker prohibition is absolute for this increment.

    Do NOT:
    - start Docker;
    - run docker;
    - run docker info;
    - run docker compose;
    - probe Docker daemon;
    - start local Supabase;
    - start containerized PostgreSQL;
    - invoke any command that contacts Docker.

    Use only non-Docker validation paths.

    If a validation command would necessarily contact Docker:
    - do not run it;
    - report that specific validation as unavailable under Founder constraint.

    Completion report must explicitly confirm:

    DOCKER WAS NOT CONTACTED.
  </docker_prohibition>

  <security>
    Never expose:
    - CE_DATABASE_URL;
    - Supabase service-role credentials;
    - admin keys;
    - passwords;
    - tokens;
    - connection strings;
    - environment secrets.

    Use existing hardened repository patterns for any authorized hosted database validation.

    Do not place secrets in argv.

    Do not permit error output to echo secrets.

    Do not broaden privileged runtime architecture.

    No new service-role-equivalent client runtime principal is authorized.
  </security>

  <phase_21_regression>
    Run all available repository validation that complies with the Docker prohibition.

    Include, where supported:
    - relevant migration validation;
    - new External Delivery pgTAP suites;
    - existing pgTAP regression suites;
    - read-contract tests;
    - authenticated probes;
    - application tests;
    - typecheck;
    - lint;
    - build;
    - verify:full or its non-Docker-supported equivalent;
    - ownership-manifest validation;
    - institutional repository validation.

    Distinguish:
    - PASS;
    - External Delivery regression;
    - pre-existing unrelated failure;
    - unavailable because Docker is prohibited.

    Repair all in-scope failures.

    Do not repair unrelated systems.
  </phase_21_regression>

  <phase_22_independent_inspection>
    Before committing application implementation, perform an independent architecture/diff inspection.

    Confirm:

    - institutional admission exists and is committed;
    - ADR-0024 matches accepted decisions;
    - no applied migration changed;
    - corrections A–F are implemented;
    - current maturity/posture are derived;
    - current scoping disposition is derived;
    - paid_scoping_indicated remains open;
    - exactly six independent readiness gates are persisted;
    - three readiness concerns are derived;
    - provider identity/detail read separation exists;
    - same-party record_class consistency exists;
    - Providers is /providers;
    - no universal ratings exist;
    - no automatic provider selection exists;
    - no automatic maturity advancement exists;
    - no automatic Commercial Proposal creation exists;
    - no automatic Work Product/Deliverable creation exists;
    - QuickBooks boundary remains intact;
    - no Client Platform changes exist;
    - no public website changes exist;
    - no OSA changes exist;
    - no engagement chronology vocabulary changes exist;
    - no storage architecture changes exist;
    - no unrelated Corporate Operations changes exist;
    - four unrelated untracked application files remain untouched;
    - Docker was never contacted.

    Repair in-scope defects and rerun affected validation before closeout.
  </phase_22_independent_inspection>

  <explicit_exclusions>
    Do NOT implement or modify:

    - enterprise procurement;
    - provider marketplace;
    - public provider directory;
    - referral network;
    - Tucson Digital Deals integration;
    - RFQ functionality;
    - RFP functionality;
    - purchase orders;
    - accounts payable;
    - provider payment;
    - provider invoicing;
    - generalized contract management;
    - generalized project management;
    - workforce scheduling;
    - time tracking;
    - utilization planning;
    - automatic provider selection;
    - AI provider matching;
    - AI provider ranking;
    - provider portal;
    - generalized skills taxonomy;
    - workflow engine;
    - document-management system;
    - QuickBooks integration;
    - sourcing SLA;
    - Client Platform;
    - CP-07;
    - public website;
    - OSA;
    - engagement chronology vocabulary;
    - storage architecture;
    - unrelated Corporate Operations systems.

    Do not create a new organizational function.

    Do not create a new institutional domain absent a Founder determination.
  </explicit_exclusions>

  <commit_strategy>
    Preserve clean institutional provenance.

    Institutional repository:
    - institutional admission should be its own bounded commit;
    - push it before application implementation begins.

    Application repository:
    - implement External Delivery as one coherent bounded increment;
    - inspect diff;
    - stage only intended External Delivery files;
    - do not stage the four unrelated untracked files;
    - commit;
    - push through normal upstream if available.

    If repository conventions materially favor multiple application commits for migration/application/test sequencing, a small coherent series is acceptable, but report every SHA and identify the final implementation baseline.

    Do not mix unrelated work.
  </commit_strategy>

  <required_completion_report>
    Return one document titled:

    External Delivery — Generation One Institutional Admission &amp; Technical Implementation — Completion Report

    Include:

    1. Institutional Repository
       - repository path;
       - branch;
       - starting SHA;
       - origin/main starting relationship.

    2. Institutional Admission Result
       - standard identifier;
       - standard path;
       - Founder determination identifier;
       - determination path;
       - documentation-index change;
       - metadata-manifest change;
       - capability-registry determination and change if required;
       - validation result;
       - admission commit SHA;
       - final origin/main relationship.

    3. Institutional Doctrine Fidelity
       Confirm no new organizational function/domain or unapproved doctrine was invented.

    4. Application Repository
       - path;
       - branch;
       - accepted baseline;
       - actual starting SHA;
       - relationship to accepted baseline;
       - origin/main starting relationship;
       - starting worktree state.

    5. Baseline Reconciliation
       Describe any intervening compatible changes or state NONE.

    6. ADR-0024
       - path;
       - status;
       - decisions recorded.

    7. Migrations Added
       - exact filenames;
       - purpose of each;
       - confirmation no applied migration was edited.

    8. Schema / Domain Implementation
       - provider identity;
       - provider detail;
       - capability;
       - provider capability;
       - evidence;
       - evaluation;
       - evaluation citations;
       - scoping;
       - requirements;
       - capabilities;
       - unknowns;
       - candidate consideration;
       - dispositions;
       - assignments;
       - readiness;
       - events;
       - performance evidence.

    9. Corrections A–F
       Explain exact implementation of each.

    10. RLS / Least Privilege
        Include provider identity/detail separation and assignment-scoped minimum read.

    11. Provenance / Append-Only Invariants.

    12. Capability Maturity Enforcement
        Include Tested and Proven evidentiary requirements and derived current state.

    13. Scoping &amp; Sourcing
        Include derived current disposition and paid_scoping_indicated treatment.

    14. Planning Economics / QuickBooks Boundary.

    15. External Assignment / Authorized Work Locality.

    16. Readiness
        - six persisted concerns;
        - three derived concerns;
        - overall readiness derivation.

    17. Provider Completion / Huerta Group Acceptance / Rework.

    18. Work Product Linkage Boundary.

    19. Provider Performance Learning.

    20. Headquarters Surfaces
        - /providers;
        - provider detail/actions;
        - Possible Work Scoping &amp; Sourcing;
        - External Assignment surfaces;
        - guides/navigation/return routing.

    21. Read-Contract Coverage.

    22. pgTAP Results
        - new suites;
        - relevant regression totals/results.

    23. Synthetic Probe Results
        - Capability Network;
        - Scoping &amp; Sourcing;
        - External Assignment.

    24. Build / Typecheck / Lint Results.

    25. Ownership Manifest Result.

    26. Known Client Platform cp_material_retrieval_* Defect
        - whether still present;
        - exact observed result;
        - confirmation it was not modified.

    27. Regression Result
        Distinguish in-scope success from unrelated pre-existing failure.

    28. Docker Compliance
        State explicitly whether any Docker command or daemon contact occurred.
        Required successful statement:
        "Docker was not contacted."

    29. Unrelated Untracked Files
        Confirm ADMIN01 counsel package, two OSA .docx files and iDoRecruit CSV were untouched/uncommitted.

    30. Explicit Exclusions
        Confirm excluded systems remained untouched.

    31. Deviations
        State NONE if none.
        If any implementation detail deviated from accepted architecture, identify exact reason and whether semantics remained unchanged.

    32. Application Commit(s)
        - exact SHA(s);
        - final full HEAD SHA;
        - origin/main relationship.

    33. Final Worktree State
        Distinguish expected pre-existing untracked files from implementation residue.

    34. Platform &amp; Systems Development Validation Disposition.

    35. Recommendation to Founder Office.
  </required_completion_report>

  <completion_gate>
    Do not recommend Founder acceptance unless:

    - institutional admission completed first;
    - institutional admission is validated and committed;
    - no Founder-reserved decision was invented;
    - ADR-0024 is correctly recorded;
    - accepted architecture A–F is implemented;
    - migrations are forward-only;
    - applied migrations are untouched;
    - RLS is forced;
    - no ordinary DELETE exists;
    - provenance is server-derived;
    - evidence/evaluation/disposition/event history is append-only;
    - provider identity/detail separation is enforced;
    - same-party record_class consistency is enforced;
    - maturity/posture are derived from evaluation history;
    - Tested/Proven evidence requirements are enforced;
    - performance evidence does not auto-promote maturity;
    - scoping disposition is derived;
    - paid_scoping_indicated remains open;
    - Commercial Proposal remains separate;
    - exactly six independent readiness concerns are persisted;
    - three readiness concerns are derived;
    - unresolved readiness cannot pass;
    - assignment locality/eligibility is enforced;
    - provider completion remains separate from acceptance;
    - rework history is durable;
    - acceptance does not auto-create Work Product/Deliverable/client state;
    - planning economics remain non-accounting;
    - /providers is implemented;
    - Scoping &amp; Sourcing is contextual to Possible Work;
    - External Assignments are contextual to authorized Engagement work;
    - read fidelity is preserved;
    - synthetic probes pass;
    - External Delivery introduces no new regression;
    - known unrelated Client Platform defect is not modified;
    - Docker was not contacted;
    - four unrelated untracked files remain untouched;
    - excluded systems remain untouched.
  </completion_gate>

  <blockers>
    If institutional repository inspection reveals a genuinely unresolved Founder-reserved classification/admission decision:

    EXTERNAL DELIVERY GENERATION ONE IMPLEMENTATION BLOCKED — FOUNDER INSTITUTIONAL DETERMINATION REQUIRED: [precise decision]

    If application baseline materially conflicts with accepted architecture:

    EXTERNAL DELIVERY GENERATION ONE IMPLEMENTATION BLOCKED — APPLICATION BASELINE RECONCILIATION REQUIRED: [precise conflict]

    If implementation encounters a technical impossibility that would require violating accepted architecture or an explicit exclusion:

    EXTERNAL DELIVERY GENERATION ONE IMPLEMENTATION BLOCKED — [precise technical reason]

    Do not substitute architecture invention for a required Founder decision.
  </blockers>

  <required_conclusion>
    If institutional admission and application implementation complete successfully, conclude exactly:

    EXTERNAL DELIVERY GENERATION ONE INSTITUTIONAL ADMISSION AND IMPLEMENTATION COMPLETE — RECOMMEND FOUNDER ACCEPTANCE REVIEW
  </required_conclusion>

  <final_instruction>
    Execute the Founder-authorized External Delivery Generation One increment.

    Institutional admission MUST occur first.

    Record the accepted institutional decision using existing institutional repository conventions.

    Do not create a new organizational function or institutional domain.

    Then implement the accepted technical architecture in huerta-group-client-engagement with corrections A–F controlling.

    Preserve:

    Capability Network
    →
    Implementation Scoping &amp; Sourcing
    →
    External Delivery Assignment
    →
    Provider Performance Learning.

    Preserve every controlling semantic distinction.

    Keep current maturity/posture and scoping disposition derived from authoritative history.

    Keep paid_scoping_indicated open.

    Persist six readiness gates and derive the other three.

    Separate provider identity from provider detail.

    Use Providers at /providers.

    Keep planning economics outside accounting truth.

    Keep External Assignment subordinate to existing Authorized Work.

    Keep provider completion separate from Huerta Group acceptance.

    Keep performance evidence separate from maturity advancement.

    Create ADR-0024.

    Use forward-only migrations.

    Preserve RLS, no-DELETE, provenance, append-only history, authentic/synthetic isolation, read fidelity and existing authority semantics.

    Use synthetic records for probes.

    Do not fabricate authentic client/provider state.

    Do not repair the unrelated Client Platform cp_material_retrieval_* defect.

    Do not touch the four unrelated untracked files.

    DO NOT CONTACT DOCKER.

    Do not begin any subsequent External Delivery increment.

    Return the complete institutional and technical implementation evidence to Founder Office for acceptance review.
  </final_instruction>

</claude_execution_package>
